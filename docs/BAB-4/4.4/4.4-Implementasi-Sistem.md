# 1. Rancangan Tampilan Masuk Akun

Gambar 4.1 Rancangan Tampilan Masuk Akun

Gambar diatas ini menunjukkan tampilan halaman masuk akun saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<div class="flex h-[100vh] flex-col items-center justify-center gap-10">
    <div class="flex flex-col items-center gap-5">
        <!-- Logo Aplikasi -->
        <img class="w-max" src="{{ vite()->asset('images/logo.svg') }}" alt="{{ config('app.name') }}" />

        <div class="box-shadow flex min-w-[300px] flex-col rounded-md bg-white dark:bg-gray-900">
            <!-- Formulir Masuk -->
            <x-admin::form :action="route('admin.session.store')">
                <p class="p-4 text-xl font-bold text-gray-800 dark:text-white">
                    @lang('admin::app.users.login.title')
                </p>

                <div class="border-y p-4 dark:border-gray-800">
                    <!-- Input Email -->
                    <x-admin::form.control-group>
                        <x-admin::form.control-group.label class="required">
                            @lang('admin::app.users.login.email')
                        </x-admin::form.control-group.label>

                        <x-admin::form.control-group.control
                            type="email"
                            class="w-[254px] max-w-full"
                            id="email"
                            name="email"
                            rules="required|email"
                            :label="trans('admin::app.users.login.email')"
                            :placeholder="trans('admin::app.users.login.email')"
                        />
                    </x-admin::form.control-group>

                    <!-- Input Kata Sandi -->
                    <x-admin::form.control-group class="relative w-full">
                        <x-admin::form.control-group.label class="required">
                            @lang('admin::app.users.login.password')
                        </x-admin::form.control-group.label>

                        <x-admin::form.control-group.control
                            type="password"
                            class="w-[254px] max-w-full"
                            id="password"
                            name="password"
                            rules="required|min:6"
                            :label="trans('admin::app.users.login.password')"
                            :placeholder="trans('admin::app.users.login.password')"
                        />
                    </x-admin::form.control-group>
                </div>

                <div class="flex items-center justify-between p-4">
                    <!-- Tautan Lupa Kata Sandi -->
                    <a class="cursor-pointer text-xs font-semibold leading-6 text-brandColor" href="{{ route('admin.forgot_password.create') }}">
                        @lang('admin::app.users.login.forget-password-link')
                    </a>

                    <!-- Tombol Masuk -->
                    <button class="primary-button">
                        @lang('admin::app.users.login.submit-btn')
                    </button>
                </div>
            </x-admin::form>
        </div>
    </div>
</div>
```

# 2. Rancangan Tampilan Form Prospek

Gambar 4.2 Rancangan Tampilan Form Prospek

Gambar diatas ini menunjukkan tampilan halaman form tambah prospek saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::form :action="route('admin.leads.store')">
    <div class="flex flex-col gap-4">
        <div class="flex items-center justify-between rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300">
            <div class="flex flex-col gap-2">
                <!-- Breadcrumb Navigasi -->
                <x-admin::breadcrumbs name="leads.create" />

                <div class="text-xl font-bold dark:text-white">
                    @lang('admin::app.leads.create.title')
                </div>
            </div>

            <div class="flex items-center gap-x-2.5">
                <!-- Tombol Simpan -->
                <button type="submit" class="primary-button">
                    @lang('admin::app.leads.create.save-btn')
                </button>
            </div>
        </div>

        <!-- Komponen Vue untuk Form Prospek -->
        <v-lead-create>
            <x-admin::shimmer.leads.datagrid />
        </v-lead-create>
    </div>
</x-admin::form>

<script type="text/x-template" id="v-lead-create-template">
    <div class="box-shadow flex flex-col gap-4 rounded-lg border border-gray-200 bg-white dark:border-gray-800 dark:bg-gray-900">
        <div class="flex w-full gap-2 border-b border-gray-200 dark:border-gray-800">
            <!-- Tab Navigasi -->
            <template v-for="tab in tabs" :key="tab.id">
                <a :href="'#' + tab.id"
                   :class="['inline-block px-3 py-2.5 border-b-2 text-sm font-medium', activeTab === tab.id ? 'text-brandColor border-brandColor' : 'text-gray-600 border-transparent']"
                   @click="scrollToSection(tab.id)"
                   :text="tab.label">
                </a>
            </template>
        </div>

        <div class="flex flex-col gap-4 px-4 py-2">
            <!-- Bagian Detail Prospek -->
            <div class="flex flex-col gap-4" id="lead-details">
                <div class="flex flex-col gap-1">
                    <p class="text-base font-semibold dark:text-white">@lang('admin::app.leads.create.details')</p>
                    <p class="text-gray-600 dark:text-white">@lang('admin::app.leads.create.details-info')</p>
                </div>

                <div class="w-1/2 max-md:w-full">
                    <!-- Atribut Kustom Prospek -->
                    <x-admin::attributes :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere([['code', 'NOTIN', ['lead_value', 'lead_type_id', 'lead_source_id', 'expected_close_date', 'user_id', 'lead_pipeline_id', 'lead_pipeline_stage_id']], 'entity_type' => 'leads', 'quick_add' => 1])" />
                </div>
            </div>

            <!-- Bagian Kontak Person -->
            <div class="flex flex-col gap-4" id="contact-person">
                <div class="flex flex-col gap-1">
                    <p class="text-base font-semibold dark:text-white">@lang('admin::app.leads.create.contact-person')</p>
                    <p class="text-gray-600 dark:text-white">@lang('admin::app.leads.create.contact-info')</p>
                </div>
                <div class="w-1/2 max-md:w-full">
                    @include('admin::leads.common.contact')
                </div>
            </div>

            <!-- Bagian Produk -->
            <div class="flex flex-col gap-4" id="products">
                <div class="flex flex-col gap-1">
                    <p class="text-base font-semibold dark:text-white">@lang('admin::app.leads.create.products')</p>
                    <p class="text-gray-600 dark:text-white">@lang('admin::app.leads.create.products-info')</p>
                </div>
                <div>
                    @include('admin::leads.common.products')
                </div>
            </div>
        </div>
    </div>
</script>
```

# 3. Rancangan Tampilan Form Penawaran

Gambar 4.3 Rancangan Tampilan Form Penawaran

Gambar diatas ini menunjukkan tampilan halaman form tambah penawaran saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::form :action="route('admin.quotes.store')">
    <div class="flex flex-col gap-4">
        <div class="flex items-center justify-between rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300">
            <div class="flex flex-col gap-2">
                <!-- Breadcrumb Navigasi -->
                <x-admin::breadcrumbs name="quotes.create" />

                <div class="text-xl font-bold dark:text-white">
                    @lang('admin::app.quotes.create.title')
                </div>
            </div>

            <div class="flex items-center gap-x-2.5">
                <!-- Tombol Simpan -->
                <button type="submit" class="primary-button">
                    @lang('admin::app.quotes.create.save-btn')
                </button>
            </div>
        </div>

        <!-- Komponen Vue untuk Form Penawaran -->
        <v-quote :errors="errors">
            <x-admin::shimmer.quotes />
        </v-quote>
    </div>
</x-admin::form>

<script type="text/x-template" id="v-quote-template">
    <div class="box-shadow flex flex-col gap-4 rounded-lg border border-gray-200 bg-white dark:border-gray-800 dark:bg-gray-900">
        <div class="flex w-full gap-2 border-b border-gray-200 dark:border-gray-800">
            <!-- Tab Navigasi -->
            <template v-for="tab in tabs" :key="tab.id">
                <a :href="'#' + tab.id"
                   :class="['inline-block px-3 py-2.5 border-b-2 text-sm font-medium', activeTab === tab.id ? 'text-brandColor border-brandColor' : 'text-gray-600 border-transparent']"
                   @click="scrollToSection(tab.id)"
                   :text="tab.label">
                </a>
            </template>
        </div>

        <div class="flex flex-col gap-4 px-4 py-2">
            <!-- Informasi Penawaran -->
            <div id="quote-info" class="flex flex-col gap-4">
                <div class="flex flex-col gap-1">
                    <p class="text-base font-semibold text-gray-800 dark:text-white">@lang('admin::app.quotes.create.quote-info')</p>
                    <p class="text-sm text-gray-600 dark:text-white">@lang('admin::app.quotes.create.quote-info-info')</p>
                </div>

                <div class="w-1/2 max-md:w-full">
                    <x-admin::attributes :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere(['entity_type' => 'quotes', ['code', 'IN', ['subject']]])" />
                    <x-admin::attributes :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere(['entity_type' => 'quotes', ['code', 'IN', ['description']]])" />
                    
                    <div class="flex gap-4">
                        <x-admin::attributes :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere(['entity_type' => 'quotes', ['code', 'IN', ['expired_at', 'user_id']]])" />
                    </div>
                    
                    <div class="flex gap-4">
                        <x-admin::attributes :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere(['entity_type' => 'quotes', ['code', 'IN', ['person_id']]])" />
                        
                        <!-- Lookup ke Prospek -->
                        <x-admin::form.control-group class="w-full">
                            <x-admin::form.control-group.label>@lang('admin::app.quotes.create.link-to-lead')</x-admin::form.control-group.label>
                            <v-lookup-component :attribute="{'code': 'lead_id', 'name': 'Lead', 'lookup_type': 'leads'}" can-add-new="true"></v-lookup-component>
                        </x-admin::form.control-group>
                    </div>
                </div>
            </div>

            <!-- Informasi Alamat -->
            <div id="address-info" class="flex flex-col gap-4">
                <div class="flex flex-col gap-1">
                    <p class="text-base font-semibold text-gray-800 dark:text-white">@lang('admin::app.quotes.create.address-info')</p>
                    <p class="text-sm text-gray-600 dark:text-white">@lang('admin::app.quotes.create.address-info-info')</p>
                </div>
                <div class="w-1/2 max-md:w-full">
                    <x-admin::attributes :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere(['entity_type' => 'quotes', ['code', 'IN', ['billing_address', 'shipping_address']]])" />
                </div>
            </div>

            <!-- Informasi Item Penawaran -->
            <div id="quote-items" class="flex flex-col gap-4">
                <div class="flex items-start justify-between">
                    <div class="flex flex-col gap-1">
                        <p class="text-base font-semibold text-gray-800 dark:text-white">@lang('admin::app.quotes.create.quote-items')</p>
                        <p class="text-sm text-gray-600 dark:text-white">@lang('admin::app.quotes.create.quote-item-info')</p>
                    </div>
                    <!-- Tombol Tambah Item -->
                    <span class="text-md flex max-w-max cursor-pointer items-center gap-2 text-brandColor" @click="addProduct">
                        @lang('admin::app.quotes.create.add-item')
                    </span>
                </div>
                <v-quote-item-list ref="quoteItemList" :errors="errors"></v-quote-item-list>
            </div>
        </div>
    </div>
</script>
```

# 4. Rancangan Tampilan Form Organisasi

Gambar 4.4 Rancangan Tampilan Form Organisasi

Gambar diatas ini menunjukkan tampilan halaman form tambah organisasi saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::form :action="route('admin.contacts.organizations.store')" method="POST">
    <div class="flex flex-col gap-4">
        <div class="flex items-center justify-between rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300">
            <div class="flex flex-col gap-2">
                <!-- Breadcrumb Navigasi -->
                <x-admin::breadcrumbs name="contacts.organizations.create" />

                <div class="text-xl font-bold dark:text-gray-300">
                    @lang('admin::app.contacts.organizations.create.title')
                </div>
            </div>

            <div class="flex items-center gap-x-2.5">
                <!-- Tombol Simpan -->
                <button type="submit" class="primary-button">
                    @lang('admin::app.contacts.organizations.create.save-btn')
                </button>
            </div>
        </div>

        <!-- Atribut Organisasi -->
        <div class="box-shadow rounded-lg border border-gray-200 bg-white p-4 dark:border-gray-800 dark:bg-gray-900">
            <x-admin::attributes
                :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere([
                    'entity_type' => 'organizations',
                ])"
                :custom-validations="[
                    'name' => ['max:100'],
                    'address' => ['max:100'],
                    'postcode' => ['postcode'],
                ]"
            />
        </div>
    </div>
</x-admin::form>
```

# 5. Rancangan Tampilan Form Kontak Individu

Gambar 4.5 Rancangan Tampilan Form Kontak Individu

Gambar diatas ini menunjukkan tampilan halaman form tambah kontak individu saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::form :action="route('admin.contacts.persons.store')" enctype="multipart/form-data">
    <div class="flex flex-col gap-4">
        <!-- Header Halaman -->
        <div class="flex items-center justify-between rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300">
            <div class="flex flex-col gap-2">
                <!-- Breadcrumb Navigasi -->
                <x-admin::breadcrumbs name="contacts.persons.create" />

                <div class="text-xl font-bold dark:text-white">
                    @lang('admin::app.contacts.persons.create.title')
                </div>
            </div>

            <div class="flex items-center gap-x-2.5">
                <!-- Tombol Simpan -->
                <button type="submit" class="primary-button">
                    @lang('admin::app.contacts.persons.create.save-btn')
                </button>
            </div>
        </div>

        <!-- Kolom Formulir -->
        <div class="box-shadow rounded-lg border border-gray-200 bg-white p-4 dark:border-gray-800 dark:bg-gray-900">
            <x-admin::attributes
                :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere([
                    ['code', 'NOTIN', ['organization_id']],
                    'entity_type' => 'persons',
                ])"
                :custom-validations="[
                    'name' => ['min:2', 'max:100'],
                    'job_title' => ['max:100'],
                ]"
            />

            <!-- Komponen Organisasi -->
            <v-organization></v-organization>
        </div>
    </div>
</x-admin::form>

<script type="text/x-template" id="v-organization-template">
    <div>
        <x-admin::attributes
            :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere([
                ['code', 'IN', ['organization_id']],
                'entity_type' => 'persons',
            ])"
        />

        <template v-if="organizationName">
            <x-admin::form.control-group.control
                type="hidden"
                name="organization_name"
                v-model="organizationName"
            />
        </template>
    </div>
</script>
```

# 6. Rancangan Tampilan Kelola Tanda Tangan

Gambar 4.6 Rancangan Tampilan Kelola Tanda Tangan

Gambar diatas ini menunjukkan tampilan halaman kelola tanda tangan saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::layouts>
    <x-slot:title>Kelola Tanda Tangan</x-slot:title>

    <div class="flex-col gap-4">
        <!-- Header Halaman -->
        <div class="flex items-center justify-between rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300">
            <div class="flex flex-col gap-2">
                <div class="flex cursor-pointer items-center">
                    <x-admin::breadcrumbs name="settings.signatures" />
                </div>
                <div class="text-xl font-bold dark:text-white">Kelola Tanda Tangan Digital</div>
            </div>
        </div>

        <div class="mt-3.5 flex gap-2.5 max-xl:flex-wrap">
            {{-- Panel Kiri: Daftar --}}
            <div class="flex flex-1 flex-col gap-2 max-xl:flex-auto">
                <div class="box-shadow rounded bg-white p-4 dark:bg-gray-900">
                    <h3 class="mb-4 text-base font-semibold text-gray-800 dark:text-white">Daftar Tanda Tangan</h3>
                    <div class="table-responsive grid w-full">
                        <table class="w-full">
                            <thead>
                                <tr class="border-b-[1px] border-gray-300 dark:border-gray-800">
                                    <th class="p-2 text-left">Nama Tanda Tangan</th>
                                    <th class="p-2 text-left">Nama Pemilik</th>
                                    <th class="p-2 text-center">Pratinjau</th>
                                    <th class="p-2 text-center">Status</th>
                                    <th class="p-2 text-right">Aksi</th>
                                </tr>
                            </thead>
                            <tbody>
                                @forelse ($signatures as $signature)
                                    <tr class="border-b-[1px] border-gray-300 dark:border-gray-800">
                                        <td class="p-2">{{ $signature->name }}</td>
                                        <td class="p-2">{{ $signature->owner_name }}</td>
                                        <td class="p-2 text-center">
                                            <img src="{{ $signature->image_url }}" class="mx-auto h-10 w-auto" style="max-width: 100px;">
                                        </td>
                                        <td class="p-2 text-center">
                                            @if ($signature->is_active)
                                                <span class="label-active">Aktif</span>
                                            @else
                                                <span class="label-inactive">Tidak Aktif</span>
                                            @endif
                                        </td>
                                        <td class="p-2 text-right">
                                            <div class="flex justify-end gap-2">
                                                @if (! $signature->is_active)
                                                    <form action="{{ route('admin.settings.signatures.set_active', $signature->id) }}" method="POST">
                                                        @csrf
                                                        <button type="submit" class="text-blue-600 hover:underline">Jadikan Aktif</button>
                                                    </form>
                                                @endif
                                                <form action="{{ route('admin.settings.signatures.destroy', $signature->id) }}" method="POST" onsubmit="return confirm('Hapus?')">
                                                    @csrf @method('DELETE')
                                                    <button type="submit" class="text-red-600 hover:underline">Hapus</button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                @empty
                                    <tr><td colspan="5" class="p-4 text-center">Belum ada tanda tangan.</td></tr>
                                @endforelse
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            {{-- Panel Kanan: Formulir --}}
            <div class="flex w-[360px] max-w-full flex-col gap-2 max-sm:w-full">
                <div class="box-shadow rounded bg-white p-4 dark:bg-gray-900">
                    <h3 class="mb-4 text-base font-semibold text-gray-800 dark:text-white">Tambah Tanda Tangan Baru</h3>
                    <x-admin::form :action="route('admin.settings.signatures.store')" enctype="multipart/form-data">
                        <x-admin::form.control-group>
                            <x-admin::form.control-group.label class="required">Nama Tanda Tangan</x-admin::form.control-group.label>
                            <x-admin::form.control-group.control type="text" name="name" rules="required" placeholder="Contoh: Tanda Tangan Direktur" />
                        </x-admin::form.control-group>

                        <x-admin::form.control-group>
                            <x-admin::form.control-group.label class="required">Nama Pemilik</x-admin::form.control-group.label>
                            <x-admin::form.control-group.control type="text" name="owner_name" rules="required" placeholder="Contoh: Budi Santoso" />
                        </x-admin::form.control-group>

                        <x-admin::form.control-group>
                            <x-admin::form.control-group.label class="required">File Gambar</x-admin::form.control-group.label>
                            <x-admin::media.images name="image" :uploaded-images="[]" />
                        </x-admin::form.control-group>

                        <button type="submit" class="primary-button w-full">Simpan Tanda Tangan</button>
                    </x-admin::form>
                </div>
            </div>
        </div>
    </div>
</x-admin::layouts>
```

# 7. Rancangan Tampilan Form Produk

Gambar 4.7 Rancangan Tampilan Form Produk

Gambar diatas ini menunjukkan tampilan halaman form tambah produk saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::form :action="route('admin.products.store')" method="POST">
    <div class="flex flex-col gap-4">
        <div class="flex items-center justify-between rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300">
            <div class="flex flex-col gap-2">
                <!-- Breadcrumb Navigasi -->
                <x-admin::breadcrumbs name="products.create" />
                
                <div class="text-xl font-bold dark:text-white">
                    @lang('admin::app.products.create.title')
                </div>
            </div>

            <div class="flex items-center gap-x-2.5">
                <!-- Tombol Simpan -->
                <button type="submit" class="primary-button">
                    @lang('admin::app.products.create.save-btn')
                </button>
            </div>
        </div>

        <div class="flex gap-2.5 max-xl:flex-wrap">
            <!-- Sub-komponen Kiri: Umum -->
            <div class="flex flex-1 flex-col gap-2 max-xl:flex-auto">
                <div class="box-shadow rounded-lg border border-gray-200 bg-white p-4 dark:border-gray-800 dark:bg-gray-900">
                    <p class="mb-4 text-base font-semibold text-gray-800 dark:text-white">
                        @lang('admin::app.products.create.general')
                    </p>

                    <x-admin::attributes
                        :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere([
                            'entity_type' => 'products',
                            ['code', 'NOTIN', ['price', 'quantity']],
                        ])"
                    />
                </div>
            </div>

            <!-- Sub-komponen Kanan: Harga -->
            <div class="flex w-[360px] max-w-full flex-col gap-2 max-sm:w-full">
                <x-admin::accordion>
                    <x-slot:header>
                        <div class="flex items-center justify-between">
                            <p class="p-2.5 text-base font-semibold text-gray-800 dark:text-white">
                                @lang('admin::app.products.create.price')
                            </p>
                        </div>
                    </x-slot>

                    <x-slot:content>
                        <x-admin::attributes
                            :custom-attributes="app('Webkul\Attribute\Repositories\AttributeRepository')->findWhere([
                                'entity_type' => 'products',
                                ['code', 'IN', ['price', 'quantity']],
                            ])"
                        />
                    </x-slot>
                </x-admin::accordion>
            </div>
        </div>
    </div>
</x-admin::form>
```

# 8. Rancangan Tampilan Dashboard

Gambar 4.8 Rancangan Tampilan Dashboard

Gambar diatas ini menunjukkan tampilan halaman dashboard saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::layouts>
    <x-slot:title>
        @lang('admin::app.dashboard.index.title')
    </x-slot>

    <div class="mb-5 flex items-center justify-between gap-4 max-sm:flex-wrap">
        <div class="grid gap-1.5">
            <p class="text-2xl font-semibold dark:text-white">
                @lang('admin::app.dashboard.index.title')
            </p>
        </div>

        <!-- Filter Dashboard -->
        <v-dashboard-filters>
            <div class="flex gap-1.5">
                <div class="light-shimmer-bg dark:shimmer h-[39px] w-[140px] rounded-md"></div>
                <div class="light-shimmer-bg dark:shimmer h-[39px] w-[140px] rounded-md"></div>
            </div>
        </v-dashboard-filters>
    </div>

    <div class="mt-3.5 flex gap-4 max-xl:flex-wrap">
        <!-- Bagian Kiri -->
        <div class="flex flex-1 flex-col gap-4 max-xl:flex-auto">
            <!-- Statistik Pendapatan -->
            @include('admin::dashboard.index.revenue')

            <!-- Statistik Keseluruhan -->
            @include('admin::dashboard.index.over-all')

            <!-- Statistik Total Prospek -->
            @include('admin::dashboard.index.total-leads')

            <div class="flex gap-4 max-lg:flex-wrap">
                <!-- Produk Terlaris -->
                @include('admin::dashboard.index.top-selling-products')

                <!-- Kontak Teratas -->
                @include('admin::dashboard.index.top-persons')
            </div>
        </div>

        <!-- Bagian Kanan -->
        <div class="flex w-[378px] max-w-full flex-col gap-4 max-sm:w-full">
            <!-- Prospek Berdasarkan Tahapan -->
            @include('admin::dashboard.index.open-leads-by-states')

            <!-- Pendapatan Berdasarkan Sumber -->
            @include('admin::dashboard.index.revenue-by-sources')

            <!-- Pendapatan Berdasarkan Tipe -->
            @include('admin::dashboard.index.revenue-by-types')
        </div>
    </div>

    <script type="text/x-template" id="v-dashboard-filters-template">
        <div class="flex gap-1.5">
            <x-admin::flat-picker.date class="!w-[140px]" ::allow-input="false" ::max-date="filters.end">
                <input class="flex min-h-[39px] w-full rounded-md border px-3 py-2 text-sm text-gray-600 transition-all hover:border-gray-400 dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300 dark:hover:border-gray-400" v-model="filters.start" placeholder="@lang('admin::app.dashboard.index.start-date')" />
            </x-admin::flat-picker.date>

            <x-admin::flat-picker.date class="!w-[140px]" ::allow-input="false" ::max-date="filters.end">
                <input class="flex min-h-[39px] w-full rounded-md border px-3 py-2 text-sm text-gray-600 transition-all hover:border-gray-400 dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300 dark:hover:border-gray-400" v-model="filters.end" placeholder="@lang('admin::app.dashboard.index.end-date')" />
            </x-admin::flat-picker.date>
        </div>
    </script>
</x-admin::layouts>
```

# 9. Rancangan Tampilan Pipeline Prospek

Gambar 4.9 Rancangan Tampilan Pipeline Prospek

Gambar diatas ini menunjukkan tampilan halaman pipeline prospek (kanban) saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<!-- Komponen Kanban Vue -->
<v-leads-kanban ref="leadsKanban">
    <div class="flex flex-col gap-4">
        <!-- Tampilan Loading (Shimmer) -->
        <x-admin::shimmer.leads.index.kanban />
    </div>
</v-leads-kanban>

<script type="text/x-template" id="v-leads-kanban-template">
    <template v-if="isLoading">
        <div class="flex flex-col gap-4">
            <x-admin::shimmer.leads.index.kanban />
        </div>
    </template>

    <template v-else>
        <div class="flex flex-col gap-4">
            @include('admin::leads.index.kanban.toolbar')

            <div class="flex gap-2.5 overflow-x-auto">
                <!-- Kartu Tahapan (Stage Cards) -->
                <div
                    class="flex min-w-[275px] max-w-[275px] flex-col gap-1 rounded-lg border border-gray-200 bg-white dark:border-gray-800 dark:bg-gray-900"
                    v-for="(stage, index) in stageLeads"
                >
                    <!-- Header Tahapan -->
                    <div class="flex flex-col px-2 py-3">
                        <div class="flex items-center justify-between">
                            <span class="text-xs font-medium dark:text-white">
                                @{{ stage.name }} (@{{ stage.leads.meta.total }})
                            </span>

                            @if (bouncer()->hasPermission('leads.create'))
                                <a
                                    :href="'{{ route('admin.leads.create') }}' + '?stage_id=' + stage.id"
                                    class="icon-add cursor-pointer rounded p-1 text-lg text-gray-600 transition-all hover:bg-gray-200 hover:text-gray-800 dark:text-gray-600 dark:hover:bg-gray-800 dark:hover:text-white"
                                    target="_blank"
                                >
                                </a>
                            @endif
                        </div>

                        <!-- Total Nilai Prospek per Tahapan -->
                        <div class="flex items-center justify-between gap-2">
                            <span class="text-xs font-medium dark:text-white">
                                @{{ $admin.formatPrice(stage.lead_value) }}
                            </span>

                            <!-- Progress Bar -->
                            <div class="h-1 w-36 overflow-hidden rounded-full bg-gray-200 dark:bg-gray-800">
                                <div
                                    class="h-1 bg-green-500"
                                    :style="{ width: (stage.lead_value / totalStagesAmount) * 100 + '%' }"
                                ></div>
                            </div>
                        </div>
                    </div>

                    <!-- Kartu Prospek yang Dapat Digeser (Draggable) -->
                    <draggable
                        class="flex h-[calc(100vh-317px)] flex-col gap-2 overflow-y-auto p-2"
                        :class="{ 'justify-center': stage.leads.data.length === 0 }"
                        ghost-class="draggable-ghost"
                        handle=".lead-item"
                        v-bind="{animation: 200}"
                        :list="stage.leads.data"
                        item-key="id"
                        group="leads"
                        @scroll="handleScroll(stage, $event)"
                        @change="handleUpdate(stage, $event)"
                    >
                        <!-- Kartu Prospek Individual -->
                        <template #item="{ element, index }">
                            <a
                                class="lead-item flex cursor-pointer flex-col gap-5 rounded-md border border-gray-100 bg-gray-50 p-2 dark:border-gray-400 dark:bg-gray-400"
                                :href="'{{ route('admin.leads.view', 'replaceId') }}'.replace('replaceId', element.id)"
                            >
                                <!-- Header Kartu -->
                                <div class="flex items-start justify-between">
                                    <div class="flex items-center gap-1">
                                        <x-admin::avatar ::name="element.person.name" />

                                        <div class="flex flex-col gap-0.5">
                                            <span class="text-xs font-medium">
                                                @{{ element.person.name }}
                                            </span>

                                            <span class="text-[10px] leading-normal">
                                                @{{ element.person.organization?.name }}
                                            </span>
                                        </div>
                                    </div>
                                </div>

                                <!-- Judul Prospek -->
                                <p class="text-xs font-medium">
                                    @{{ element.title }}
                                </p>

                                <div class="flex flex-wrap gap-1">
                                    <div class="rounded-xl bg-gray-200 px-2 py-1 text-xs font-medium dark:bg-gray-800 dark:text-white">
                                        @{{ element.formatted_lead_value }}
                                    </div>
                                </div>
                            </a>
                        </template>
                    </draggable>
                </div>
            </div>
        </div>
    </template>
</script>
```

# 10. Rancangan Tampilan Daftar Penawaran

Gambar 4.10 Rancangan Tampilan Daftar Penawaran

Gambar diatas ini menunjukkan tampilan halaman daftar penawaran saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::layouts>
    <x-slot:title>
        @lang('admin::app.quotes.index.title')
    </x-slot>

    <div class="flex flex-col gap-4">
        <div class="flex items-center justify-between rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300">
            <div class="flex flex-col gap-2">
                <!-- Breadcrumb Navigasi -->
                <x-admin::breadcrumbs name="quotes" />
    
                <div class="text-xl font-bold dark:text-white">
                    @lang('admin::app.quotes.index.title')
                </div>
            </div>
    
            <div class="flex items-center gap-x-2.5">
                <!-- Tombol Buat Penawaran -->
                <div class="flex items-center gap-x-2.5">
                    @if (bouncer()->hasPermission('quotes.create'))
                        <a href="{{ route('admin.quotes.create') }}" class="primary-button">
                            @lang('admin::app.quotes.index.create-btn')
                        </a>
                    @endif
                </div>
            </div>
        </div>
    
        <!-- Komponen DataGrid -->
        <x-admin::datagrid :src="route('admin.quotes.index')" />
    </div>
</x-admin::layouts>
```

# 11. Rancangan Tampilan Daftar Organisasi

Gambar 4.11 Rancangan Tampilan Daftar Organisasi

Gambar diatas ini menunjukkan tampilan halaman daftar organisasi saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::layouts>
    <x-slot:title>
        @lang('admin::app.contacts.organizations.index.title')
    </x-slot>

    <div class="flex flex-col gap-4">
        <div class="flex items-center justify-between rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300">
            <div class="flex flex-col gap-2">
                <!-- Breadcrumb Navigasi -->
                <x-admin::breadcrumbs name="contacts.organizations" />
                
                <div class="text-xl font-bold dark:text-gray-300">
                    @lang('admin::app.contacts.organizations.index.title')
                </div>
            </div>

            <div class="flex items-center gap-x-2.5">
                <div class="flex items-center gap-x-2.5">
                    @if (bouncer()->hasPermission('contacts.organizations.create'))
                        <!-- Tombol Buat Organisasi -->
                        <a href="{{ route('admin.contacts.organizations.create') }}" class="primary-button">
                            @lang('admin::app.contacts.organizations.index.create-btn')
                        </a>
                    @endif
                </div>
            </div>
        </div>

        <!-- Komponen DataGrid -->
        <x-admin::datagrid :src="route('admin.contacts.organizations.index')" />
    </div>
</x-admin::layouts>
```

# 12. Rancangan Tampilan Daftar Kontak Individu

Gambar 4.12 Rancangan Tampilan Daftar Kontak Individu

Gambar diatas ini menunjukkan tampilan halaman daftar kontak individu saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::layouts>
    <x-slot:title>
        @lang('admin::app.contacts.persons.index.title')
    </x-slot>

    <div class="flex flex-col gap-4">
        <div class="flex items-center justify-between rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300">
            <div class="flex flex-col gap-2">
                <x-admin::breadcrumbs name="contacts.persons" />

                <div class="text-xl font-bold dark:text-white">
                    @lang('admin::app.contacts.persons.index.title')
                </div>
            </div>

            <div class="flex items-center gap-x-2.5">
                <!-- Tombol Ekspor -->
                <x-admin::datagrid.export :src="route('admin.contacts.persons.index')" />

                <!-- Tombol Buat Kontak -->
                <div class="flex items-center gap-x-2.5">
                    @if (bouncer()->hasPermission('contacts.persons.create'))
                        <a href="{{ route('admin.contacts.persons.create') }}" class="primary-button">
                            @lang('admin::app.contacts.persons.index.create-btn')
                        </a>
                    @endif
                </div>
            </div>
        </div>

        <!-- Komponen DataGrid -->
        <x-admin::datagrid src="{{ route('admin.contacts.persons.index') }}" :isMultiRow="true" />
    </div>
</x-admin::layouts>
```

# 13. Rancangan Tampilan Analisis Market Basket

Gambar 4.13 Rancangan Tampilan Analisis Market Basket

Gambar diatas ini menunjukkan tampilan halaman analisis market basket (apriori) saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<div class="box-shadow rounded-lg border border-gray-200 bg-white p-4 dark:border-gray-800 dark:bg-gray-900 lg:col-span-1">
    <!-- Judul Formulir -->
    <div class="mb-2 text-base font-semibold">Jalankan Analisis Baru</div>

    <!-- Formulir untuk menjalankan analisis Apriori baru -->
    <x-admin::form :action="route('admin.analytics.market_basket.run')" method="POST">
        @csrf

        <!-- Input Rentang Tanggal -->
        <div class="grid grid-cols-2 gap-3">
            <div>
                <x-admin::form.control-group.label>
                    Dari
                </x-admin::form.control-group.label>
                <x-admin::form.control-group.control type="date" name="from" />
            </div>

            <div>
                <x-admin::form.control-group.label>
                    Sampai
                </x-admin::form.control-group.label>
                <x-admin::form.control-group.control type="date" name="to" />
            </div>
        </div>

        <!-- Input Parameter Apriori: Support, Confidence, Min. Item -->
        <div class="mt-3 grid grid-cols-3 gap-3">
            <div>
                <x-admin::form.control-group.label>
                    Support
                </x-admin::form.control-group.label>
                <x-admin::form.control-group.control type="number" name="support" step="0.01" min="0" max="1" value="0.05" />
            </div>

            <div>
                <x-admin::form.control-group.label>
                    Confidence
                </x-admin::form.control-group.label>
                <x-admin::form.control-group.control type="number" name="confidence" step="0.01" min="0" max="1" value="0.6" />
            </div>

            <div>
                <x-admin::form.control-group.label>
                    Min. Item
                </x-admin::form.control-group.label>
                <x-admin::form.control-group.control type="number" name="min_items" min="1" value="2" />
            </div>
        </div>

        <!-- Input Nama Label (Snapshot) -->
        <div class="mt-3">
            <x-admin::form.control-group.label>
                Nama Label (Snapshot)
            </x-admin::form.control-group.label>
            <x-admin::form.control-group.control
                type="text"
                name="label"
                placeholder="Contoh: Q1 2025 - 90 Hari"
            />
        </div>

        <!-- Opsi Tambahan -->
        <div class="mt-3 flex flex-col gap-2">
            <label class="flex items-center gap-2">
                <input type="checkbox" name="persist" value="1" class="rounded" />
                <span>Simpan Data Transaksi</span>
            </label>

            <label class="flex items-center gap-2">
                <input type="checkbox" name="activate" value="1" class="rounded" />
                <span>Jadikan rekap aktif setelah selesai</span>
            </label>
        </div>

        <!-- Tombol Submit -->
        <div class="mt-4">
            <button type="submit" class="primary-button">Proses &amp; Simpan Rules</button>
        </div>
    </x-admin::form>
</div>
```

# 14. Rancangan Tampilan Daftar Produk

Gambar 4.14 Rancangan Tampilan Daftar Produk

Gambar diatas ini menunjukkan tampilan halaman daftar produk saat dibuka, yang didapatkan dari potongan program berikut dibawah ini:

```html
<x-admin::layouts>
    <!-- Slot Judul Halaman -->
    <x-slot:title>
        @lang('admin::app.products.index.title')
    </x-slot>

    <div class="flex flex-col gap-4">
        <!-- Header Halaman -->
        <div class="flex items-center justify-between rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm dark:border-gray-800 dark:bg-gray-900 dark:text-gray-300">
            <div class="flex flex-col gap-2">
                <!-- Breadcrumb Navigasi -->
                <x-admin::breadcrumbs name="products" />

                <!-- Judul Utama -->
                <div class="text-xl font-bold dark:text-white">
                    @lang('admin::app.products.index.title')
                </div>
            </div>

            <!-- Aksi Halaman -->
            <div class="flex items-center gap-x-2.5">
                <!-- Tombol Buat Produk -->
                @if (bouncer()->hasPermission('products.create'))
                    <div class="flex items-center gap-x-2.5">
                        <a
                            href="{{ route('admin.products.create') }}"
                            class="primary-button"
                        >
                            @lang('admin::app.products.index.create-btn')
                        </a>
                    </div>
                @endif
            </div>
        </div>

        <!-- Komponen DataGrid untuk menampilkan daftar produk -->
        <x-admin::datagrid :src="route('admin.products.index')">
            <!-- Tampilan Shimmer (Loading) untuk DataGrid -->
            <x-admin::shimmer.datagrid />
        </x-admin::datagrid>
    </div>
</x-admin::layouts>
```

# 15. Rancangan Tampilan Dokumen Penawaran Harga

Gambar 4.15 Rancangan Tampilan Dokumen Penawaran Harga

Gambar diatas ini menunjukkan tampilan dokumen PDF penawaran harga yang dihasilkan, yang didapatkan dari potongan program berikut dibawah ini:

```html
<div class="page">
    <!-- Header Dokumen -->
    <div class="page-header">
        <b>@lang('admin::app.quotes.index.pdf.title')</b>
    </div>

    <div class="page-content">
        <!-- Informasi Penawaran -->
        <table>
            <tbody>
                <tr>
                    <td style="width: 50%;">
                        <b>@lang('admin::app.quotes.index.pdf.quote-id'):</b>
                        <span>#{{ $quote->id }}</span>
                    </td>
                    <td style="width: 50%;">
                        <b>@lang('admin::app.quotes.index.pdf.person'):</b>
                        <span>{{ $quote->person->name }}</span>
                    </td>
                </tr>
                <!-- ... informasi lainnya seperti sales person, subjek, tanggal, dll. -->
            </tbody>
        </table>

        <!-- Alamat Penagihan & Pengiriman -->
        <table>
            <thead>
                <tr>
                    @if ($quote->billing_address)
                        <th style="width: 50%;"><b>@lang('admin::app.quotes.index.pdf.billing-address')</b></th>
                    @endif
                    @if ($quote->shipping_address)
                        <th style="width: 50%"><b>@lang('admin::app.quotes.index.pdf.shipping-address')</b></th>
                    @endif
                </tr>
            </thead>
            <tbody>
                <tr>
                    @if ($quote->billing_address)
                        <td>
                            <!-- ... detail alamat penagihan ... -->
                        </td>
                    @endif
                    @if ($quote->shipping_address)
                        <td>
                           <!-- ... detail alamat pengiriman ... -->
                        </td>
                    @endif
                </tr>
            </tbody>
        </table>

        <!-- Tabel Item Produk -->
        <div class="items">
            <table>
                <thead>
                    <tr>
                        <th>@lang('admin::app.quotes.index.pdf.sku')</th>
                        <th>@lang('admin::app.quotes.index.pdf.product-name')</th>
                        <th>@lang('admin::app.quotes.index.pdf.price')</th>
                        <th>@lang('admin::app.quotes.index.pdf.quantity')</th>
                        <th>@lang('admin::app.quotes.index.pdf.grand-total')</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach ($quote->items as $item)
                        <tr>
                            <td>{{ $item->sku }}</td>
                            <td>{{ $item->name }}</td>
                            <td>{!! core()->formatBasePrice($item->price, true) !!}</td>
                            <td>{{ $item->quantity }}</td>
                            <td>{!! core()->formatBasePrice($item->total + $item->tax_amount - $item->discount_amount, true) !!}</td>
                        </tr>
                    @endforeach
                </tbody>
            </table>
        </div>

       <!-- Tabel Ringkasan Total -->
        <div class="summary">
            <table>
                <tbody>
                    <tr>
                        <td>@lang('admin::app.quotes.index.pdf.sub-total')</td>
                        <td>-</td>
                        <td>{!! core()->formatBasePrice($quote->sub_total, true) !!}</td>
                    </tr>
                    <!-- ... baris lain untuk pajak, diskon, penyesuaian -->
                    <tr>
                        <td><strong>@lang('admin::app.quotes.index.pdf.grand-total')</strong></td>
                        <td><strong>-</strong></td>
                        <td><strong>{!! core()->formatBasePrice($quote->grand_total, true) !!}</strong></td>
                    </tr>
                </tbody>
            </table>
        </div>

        <!-- Bagian Tanda Tangan -->
        <div style="width: 100%; clear: both; margin-top: 50px;">
            <div style="float: right; width: 200px; text-align: center;">
                <p style="margin-bottom: 10px;">Hormat Kami,</p>

                @if ($activeSignature && file_exists(public_path('storage/' . $activeSignature->image_path)))
                    <div style="height: 100px; text-align: center;">
                        <img src="{{ public_path('storage/' . $activeSignature->image_path) }}" alt="Signature" style="max-width: 150px; max-height: 100px;">
                    </div>
                @else
                    <div style="height: 100px;"></div>
                @endif

                <p style="margin-top: 10px; font-weight: bold; border-top: 1px solid #000;">
                    {{ $activeSignature->owner_name ?? '' }}
                </p>
            </div>
        </div>
    </div>
</div>
```

# 16. Rancangan Tampilan Laporan Analisis Market Basket

Gambar 4.16 Rancangan Tampilan Laporan Analisis Market Basket

Gambar diatas ini menunjukkan tampilan dokumen PDF laporan hasil analisis market basket yang dihasilkan, yang didapatkan dari potongan program berikut dibawah ini:

```html
<body>
    <!-- Header Laporan -->
    <div class="header">
        <h1>Laporan Analisis Asosiasi Produk (Market Basket Analysis)</h1>
    </div>

    <!-- Informasi Meta, berisi parameter dan periode analisis -->
    <table class="meta-info">
        <tr>
            <td class="meta-label">Nama Rekap</td>
            <td>: {{ $run->name ?? '-' }}</td>
            <td class="meta-label">Dibuat Pada</td>
            <td>: {{ $run->created_at ? $run->created_at->format('d M Y H:i') : '-' }}</td>
        </tr>
        <tr>
            <td class="meta-label">Periode Data</td>
            <td>: {{ $run->period_start ? $run->period_start->format('d M Y') : '?' }} s/d {{ $run->period_end ? $run->period_end->format('d M Y') : '?' }}</td>
            <td class="meta-label">Total Transaksi</td>
            <td>: {{ number_format($run->transactions_count) }}</td>
        </tr>
        <tr>
            <td class="meta-label">Parameter</td>
            <td colspan="3">
                : Support ≥ {{ number_format($run->support_threshold, 3) }},
                  Confidence ≥ {{ number_format($run->confidence_threshold, 3) }},
                  Min Items = {{ $run->min_items }}
            </td>
        </tr>
    </table>

    <!-- Tabel Data Hasil Analisis (Association Rules) -->
    <table class="data-table">
        <thead>
            <tr>
                <th width="35%">LHS (Jika membeli...)</th>
                <th width="35%">RHS (Maka akan membeli...)</th>
                <th width="10%">Support</th>
                <th width="10%">Confidence</th>
                <th width="10%">Lift</th>
            </tr>
        </thead>
        <tbody>
            @forelse($rules as $rule)
                <tr>
                    <!-- Kolom LHS (Left-Hand Side) -->
                    <td>{{ implode(', ', json_decode($rule->lhs, true)) }}</td>
                    
                    <!-- Kolom RHS (Right-Hand Side) -->
                    <td>{{ implode(', ', json_decode($rule->rhs, true)) }}</td>

                    <!-- Kolom Metrik Apriori -->
                    <td class="text-center">{{ number_format($rule->support, 4) }}</td>
                    <td class="text-center">{{ number_format($rule->confidence, 4) }}</td>
                    <td class="text-center">{{ number_format($rule->lift, 4) }}</td>
                </tr>
            @empty
                <tr>
                    <td colspan="5" class="text-center">Tidak ada rules yang ditemukan pada analisis ini.</td>
                </tr>
            @endforelse
        </tbody>
    </table>

    <!-- Catatan Tambahan jika data melebihi batas tampilan -->
    @if ($totalRules > $limit)
        <div class="footer-note">
            <strong>Catatan:</strong> Hanya ditampilkan {{ $limit }} dari total {{ $totalRules }} rules dengan nilai Lift tertinggi.
        </div>
    @endif

    <!-- Bagian Tanda Tangan Persetujuan -->
    <div class="footer-signature">
        <p>Mengetahui,</p>
        
        @if ($activeSignature)
            <div style="height: 100px; text-align: center;">
                <img src="{{ public_path('storage/' . $activeSignature->image_path) }}" alt="Signature" style="max-height: 100px;">
            </div>
            <p style="border-top: 1px solid #000; padding-top: 5px;">
                {{ $activeSignature->owner_name }}
            </p>
        @else
            <div style="height: 100px;"></div>
            <p style="border-top: 1px solid #000; padding-top: 5px;">
                (Belum ada tanda tangan aktif)
            </p>
        @endif
    </div>
</body>
```
